from django.contrib import admin

# register apps here that you'd like to be able to access
# via the admin menu during production
# When in settings.DEBUG == True, it registers all models
# see main.admin